import React from 'react'
import { SafeAreaView, Text } from 'react-native';

export default function Record() {
    return (
        <SafeAreaView>
            <Text>record</Text>
        </SafeAreaView>
    );
}